/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Add image domains here if using external images later (like from Sanity)
    // images: {
    //   remotePatterns: [
    //     {
    //       protocol: 'https',
    //       hostname: 'cdn.sanity.io',
    //     },
    //   ],
    // },
  };
  
  export default nextConfig;